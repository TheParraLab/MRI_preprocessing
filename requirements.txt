# General runtime requirements for MRI_preprocessing
#
# Pins match the verified 1.0 container image (pip freeze of
# mri_preprocessing, Python 3.10) — do not loosen without rebuilding the
# image and re-running the pipeline verification.
# For development and testing see `requirements-dev.txt`.

numpy==2.2.6
pandas==2.3.3
scipy==1.15.3
pydicom==3.0.2
nibabel==5.4.2

# Optional profiling for 01/02 (--profile); guarded import, pinned here so the
# feature works identically in Docker and conda deployments.
yappi==1.7.6

# Notes:
# - matplotlib / tqdm are NOT pipeline runtime dependencies; they are used by
#   diagnostic tooling (code/tools/) and are listed in requirements-dev.txt.
# - Testing and development tools (pytest, pytest-cov, etc.) are listed in
#   requirements-dev.txt.
